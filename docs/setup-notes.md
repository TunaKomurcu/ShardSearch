# Kurulum Notları

- Bu makinede `irm ... | iex` (Invoke-WebRequest/RestMethod) ile `uv` kurulumu ağ sandbox'ında sessizce donuyor — çözüm: `curl.exe` ile GitHub release zip'ini doğrudan indirip PATH'e eklemek (bkz. Faz 0 kurulum geçmişi).
